# Removed 3D Model Backup

The previous Atom 3D model files were backed up here before removal from the active project.

## Backed Up

- `atom-bot/` - previously synced model assets from `assets/models/atom-bot/`
- `atom-xbox-model3d-download/` - original downloaded model folder
- `Atom-hta-before-custom-bot.hta` - desktop UI before replacing the 3D model panel

## Active Replacement

The active desktop UI now uses a custom built-in Atom bot visual in `frontend/desktop/Atom.hta`.

