<ui:UiPage x:Class="AtomAssistant.Views.Pages.ModelsPage"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
            xmlns:local="clr-namespace:AtomAssistant.Views.Pages"
            mc:Ignorable="d"
            d:DesignHeight="800" d:DesignWidth="1200"
            Title="Models">

    <ScrollViewer>
        <StackPanel Margin="32" Spacing="24">

            <TextBlock Text="AI Models"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <ui:Card Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Cloud Models"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <ItemsControl ItemsSource="{Binding CloudModels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8"
                                        CornerRadius="6"
                                        Background="{DynamicResource CardBackgroundBrush}"
                                        BorderBrush="{DynamicResource CardBorderBrush}"
                                        BorderThickness="1"
                                        Margin="0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="200" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   Width="120" />

                                        <TextBox Grid.Column="1"
                                                 Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}"
                                                 ui:TextBoxHelper.PasswordRevealMode="Visible"
                                                 PlaceholderText="Enter API Key..."
                                                 Margin="8,0"
                                                 VerticalAlignment="Center" />

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding ModelId}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="8,0" />

                                        <ui:ToggleSwitch Grid.Column="3"
                                                         IsChecked="{Binding IsEnabled}"
                                                         Content="Enabled"
                                                         Margin="8,0,0,0" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:Card>

            <ui:Card Padding="16">
                <StackPanel Spacing="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Local Models"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <ui:Button Appearance="Primary"
                                       Icon="ScanCamera24"
                                       Content="Auto-Detect"
                                       Command="{Binding AutoDetectLocalModelsCommand}" />
                        </StackPanel>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding LocalModels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8"
                                        CornerRadius="6"
                                        Background="{DynamicResource CardBackgroundBrush}"
                                        BorderBrush="{DynamicResource CardBorderBrush}"
                                        BorderThickness="1"
                                        Margin="0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   Width="100" />

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding RamUsage, StringFormat='RAM: {0}'}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding VramUsage, StringFormat='VRAM: {0}'}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                                        <TextBlock Grid.Column="3"
                                                   Text="{Binding ContextLength, StringFormat='Context: {0:N0}'}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                                        <ui:Button Grid.Column="4"
                                                   Appearance="Subtle"
                                                   Content="{Binding IsRunning, Converter={StaticResource BoolToStartStopConverter}}"
                                                   Command="{Binding DataContext.ToggleLocalModelCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                   CommandParameter="{Binding}"
                                                   Margin="8,0" />

                                        <TextBlock Grid.Column="5"
                                                   Text="{Binding IsInstalled, Converter={StaticResource BoolToInstalledConverter}}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="8,0"
                                                   Foreground="{DynamicResource SystemFillColorSuccessBrush}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:Card>

        </StackPanel>
    </ScrollViewer>
</ui:UiPage>
