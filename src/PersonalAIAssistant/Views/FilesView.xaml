<UserControl x:Class="PersonalAIAssistant.Views.FilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Files" Style="{StaticResource SectionTitleText}" Foreground="{DynamicResource TextBrush}" />
        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Background="{DynamicResource SurfaceBrush}" AllowDrop="True">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Drop files here" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock Text="PDF, Word, Excel, CSV, images, ZIP files, and source code" Foreground="{DynamicResource MutedTextBrush}" Margin="0,8,0,16" />
                <Button Content="Choose Files" Style="{StaticResource PrimaryButton}" Width="140" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="2" Margin="0,22,0,0">
            <TextBlock Text="Supported Analysis" FontWeight="SemiBold" Margin="0,0,0,10" />
            <ItemsControl ItemsSource="{Binding SupportedFileTypes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource AccentSoftBrush}" CornerRadius="8" Padding="12,8" Margin="0,0,10,10">
                            <TextBlock Text="{Binding}" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
